Todo
--------------------------------------------------
* Add shutdown handler to clean up all memory
 + Add term-callback to philo example
 + Make sure all threads exit properly
 + Make sure all threads clean up all memory when using orderly shutdown
 + Fix thread cleanup in example simple when doing Ctrl-C
 + Document the intended usage that programs should follow
 + Fix any un-released memory at exit
* Add threadinfo to list for reaping by main thread
* Sort threads for <simple threads> in order of creation
* Add CLI config access
* Add callbacks for config setting changes
* Add REST API to access configuration and run-time values
* Add HA facilities
* Add source code spell checker
* Add splint
* Add expect tests for simple and philo to test common commands
* Add stress tests

Done
--------------------------------------------------
* Alphabetize hash tables
* Add util tests
* Add JSON saving of configuration
* Add CLOC script

